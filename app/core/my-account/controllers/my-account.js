( function (app) {

    'use strict';

    /* Controllers */

    app.controller('MyAccountController', ['$scope', '$timeout', '$mdSidenav', '$log', 'Company', '$interval',

        function ($scope, $timeout, $mdSidenav, $log, Company, $interval) {

        }
    ]);
}(app));